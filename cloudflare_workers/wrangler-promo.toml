name = "promo-code-validation"
main = "src/promo-code-validation.js"
compatibility_date = "2024-01-01"

# Secrets (set via: wrangler secret put NOCODEBACKEND_API_KEY -c wrangler-promo.toml)
# - NOCODEBACKEND_API_KEY: NocodeBackend API key
